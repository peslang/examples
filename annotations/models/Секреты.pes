// @Локально на методе файла — виден только в этом .pes.
// Соседний Program.pes может вызвать @ВПроекте обёртку, но не ВнутреннийКлюч напрямую.

@Локально
метод ВнутреннийКлюч(): Строка
    возврат "скрыто"
;

метод ПубличныйОтвет(): Строка
    возврат "ok:" + ВнутреннийКлюч()
;
